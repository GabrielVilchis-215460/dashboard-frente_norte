import { PageHeader } from '../../components/layout';
export function MapPage() {
  return (
    <div>
      <PageHeader title="Mapa del Ecosistema" description="Visualización georreferenciada de actores STEM en Ciudad Juárez" />
      <p style={{ color: 'var(--color-text-secondary)', fontSize: 'var(--text-sm)' }}>Módulo en construcción</p>
    </div>
  );
}
